# Mailjet API Configuration
# Get your API keys from: https://app.mailjet.com/account/apikeys

# Your Mailjet API public key (also called API Key)
MJ_APIKEY_PUBLIC=your_mailjet_public_api_key_here

# Your Mailjet API private key (also called Secret Key)
MJ_APIKEY_PRIVATE=your_mailjet_private_api_key_here

# Email Sender Configuration
# IMPORTANT: This email must be verified in your Mailjet account
# Go to: https://app.mailjet.com/account/sender

# The verified sender email address
SENDER_EMAIL=your-verified-email@yourdomain.com

# The name that will appear as the sender
SENDER_NAME=Your Name or Organization Name

# Instructions:
# 1. Copy this file and rename it to .env
# 2. Replace all placeholder values with your actual Mailjet credentials
# 3. Make sure your sender email is verified in Mailjet before sending emails
# 4. Never commit the .env file to version control (it should be in .gitignore)
